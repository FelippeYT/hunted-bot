[phases.setup]
nixPkgs = ["python3", "playwright-driver"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "PLAYWRIGHT_BROWSERS_PATH=$HOME/.cache/ms-playwright python3 -m playwright install chromium"
]
